Django Notes



Create A Project==========
django-admin startproject Project_Name

Rung Project================
python manage.py runserver


Create App in the project================
python manage.py startapp App_Name

python manage.py help - To get the list of Django commands


After Creating App, 
we register the new app in the setting.py file in project folder
we write view in the view files and put those view to the custom created urls file the app
we connect the urls file from the app to the urls file in the project


We normally render html files for the veiw, but occasionally use HttpResponse method for testing purpose to response

we pass request (mandatory parameter), and most of the time, another optional parameter is passed in the render functions


Using templates==========
In an App, Create templates>template_name>template_file.html.. and so on.
for eg..    templates>hello>index.html
            templates>hello>other.html
            templates>Info>index.html
            templates>Info>InfoDetails.html

etc....

Django uses Templating Languange, jinja

In HTML templates, use Kalibracess for using the passing variable from Python in the HTML 


Usign Static File===================
Static File for the CSS, Javascript
In an App, Create static>static_name>style.css


Models
Creating Model in model file in an app

python manage.py makemigrations - Establish Migration
python manage.py migrate - Migrating the database

python manage.py shell - Entering Python Shell / Django Sheel to interact with the database

from app_name.modelfile import created_model_name

Saving Data
model.save()


get all data Quering
model.objects.all()


get the first item from the table (Model in Django)
all_data  = model.objects.all()
all_data.first()


To get the propertis of a table (Model in Django)
all_data  = model.objects.all()
firstObject = all_data.first()
firstObject.property_name


To delete an object data in Django
dataObject.delete()

Filtering data objet
table.objects.filter(propertyName="Anything")

table.objects.filter(propertyName="Anything").first() - The first met object data is returned back

table.objects.get(propertyName="Anything") - Get one result


Create Super User
python manage.py createsuperuser

Register Models to admin.py in the app folder
admin.site.register(ModelName)


request.Post['name'] - Get posted data

request.method == "POST" - Check Request method

Use Form > Action Attribute and Method

Use { csrf_token } right before the submit attribute


Passenger.objects.exclude(flights=flight).all()


Some View Function does not its own actual view (html etc..). Just Functionalities
















